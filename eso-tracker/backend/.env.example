# Copy this file to .env and adjust if needed. Both lines are optional --
# the backend works with no .env file at all, using the defaults shown.

# Where the SQLite database file lives.
# Default (if unset): <this backend folder>/data/eso.db
# ESO_DB_PATH=C:\path\to\wherever\eso.db
